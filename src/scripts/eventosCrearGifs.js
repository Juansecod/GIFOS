btnInit.addEventListener("click", () => eventsBtn.init());
btnRecord.addEventListener("click", () => eventsBtn.startRecord());
btnStopRecord.addEventListener("click", () => eventsBtn.finishRecord());
btnSubmit.addEventListener("click", () => eventsBtn.submitVideo());